package step20.ex5;

// 배열 값 지정
@MyAnnotation3(v1 = {"홍길동", "임꺽정", "유관순"},
               v2 = {1000, 2000, 3000},
               v3 = {1.12f, 2.23f, 3.34f})// 필수 프로퍼티가 없는 경우, 별도로 값을 지정할 필요는 없다.

// 하지만 이건 필수 프로퍼티기때문에 값을 지정
public class MyClass4 {
    
}
